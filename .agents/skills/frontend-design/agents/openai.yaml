# 이 템플릿에서 추가한 파일이며 업스트림 원본에는 없습니다.
#
# init-agents-md / handoff 와 달리 자동 호출을 허용합니다.
# 파일을 쓰지 않는 참조용 가이던스라 UI 작업 중 알아서 걸리는 게 맞습니다.

interface:
  display_name: Frontend Design
  short_description: 템플릿 같지 않은, 의도가 담긴 UI 디자인 가이던스.

policy:
  allow_implicit_invocation: true
