# AGENTS.md

<!--
Codex 가 이 파일을 직접 읽습니다.

Codex 는 지시문을 아래 순서로 이어붙입니다. 뒤에 오는 것이 앞을 덮습니다.
  1. ~/.codex/AGENTS.override.md  (있으면 아래 2번 대신)
  2. ~/.codex/AGENTS.md           (전역 개인 설정)
  3. <git 루트>/AGENTS.override.md (있으면 아래 4번 대신)
  4. <git 루트>/AGENTS.md          (이 파일)
  5. 하위 디렉토리의 AGENTS.md     (모노레포에서 패키지별 규칙)

개인 설정으로 이 파일을 덮고 싶으면 옆에 AGENTS.override.md 를 만드세요.
gitignore 되어 있어 커밋되지 않습니다.

전체 합계가 32 KiB 를 넘으면 초과분이 조용히 잘립니다 (project_doc_max_bytes).
-->

## 프로젝트

- **한 줄 설명**: 브라우저에서 키보드로 즐기는 싱글플레이 낙하 블록 게임
- **스택**: Vite, TypeScript, HTML Canvas, Vitest, Playwright, ESLint
- **진입점**: `index.html` → `src/main.ts`

## 명령어

| 목적 | 명령 |
| --- | --- |
| 설치 | `npm install` |
| 개발 서버 | `npm run dev` |
| 빌드 | `npm run build` |
| 테스트 | `npm run test` |
| 단일 테스트 | `npm run test -- src/game/engine.test.ts` |
| 린트 / 포맷 | `npm run lint` |
| 타입 체크 | `npm run typecheck` |

명령이 바뀌면 이 표를 먼저 고칩니다. 표에 없는 명령을 추측해서 쓰지 마세요.

## 작업 규칙

- **끝났다고 말하기 전에 실제로 실행해서 확인합니다.** 빌드·테스트를 돌리고 그 결과와 함께 보고합니다. 타입 체크만 통과한 것을 "동작한다"고 말하지 않습니다.
- **실패는 실패라고 말합니다.** 테스트가 깨지면 출력과 함께 그대로 보고합니다. 건너뛴 단계가 있으면 건너뛰었다고 말합니다.
- **요청한 범위만 합니다.** 지나가다 발견한 문제는 고치지 말고 따로 언급합니다. 요청하지 않은 리팩터링, 파일 생성, 의존성 추가를 하지 않습니다.
- **주변 코드를 따라 씁니다.** 기존 파일의 네이밍, 포맷, 주석 밀도, 에러 처리 방식을 그대로 맞춥니다. 새 패턴을 도입하기 전에 이미 있는 유틸리티를 먼저 찾습니다.
- **막히면 멈추고 묻습니다.** 추측으로 진행해서 되돌리기 어려운 상태를 만들지 않습니다. 답을 기다리지 않아도 되는 부분은 먼저 끝내 둡니다.
- 커밋 메시지는 무엇을 왜 바꿨는지 한 줄로 씁니다. 장식용 접두사를 붙이지 않습니다.

## 확인이 필요한 작업

아래는 진행 전에 반드시 사용자에게 확인합니다.

- `git push`, `git push --force`, 브랜치·태그 삭제, 히스토리 재작성
- 요청 없는 커밋 (변경만 하고 커밋은 사용자가 결정합니다)
- DB 마이그레이션 실행, 스키마 변경, 시드 데이터 삭제
- 의존성 추가·업그레이드, lock 파일 갱신
- 파일·디렉토리 대량 삭제, 생성된 산출물 외의 `rm -rf`
- 배포, 외부로 나가는 요청, 퍼블리시

## 건드리지 않을 것

- `.env`, `.env.*`, `secrets/` — 읽지도, 커밋하지도, 내용을 출력하지도 않습니다.
- API 키·토큰·비밀번호를 코드나 커밋 메시지에 하드코딩하지 않습니다. 예시가 필요하면 플레이스홀더를 씁니다.
- `node_modules/`, `target/`, `dist/`, `.venv/` 등 생성물 디렉토리를 직접 수정하지 않습니다.

## 세션 인수인계

컨텍스트는 세션마다 초기화됩니다. 이어서 작업하려면 상태를 파일로 남겨야 합니다.

- 세션을 끝낼 때 `$handoff` 를 실행해 `docs/notes/handoff.md` 를 갱신합니다.
- 세션을 시작할 때 `docs/notes/handoff.md` 가 있으면 먼저 읽습니다. 거기 적힌 "검증됨/미검증" 구분을 신뢰하되, 미검증 항목은 직접 확인합니다.
- 규모 있는 작업의 계획은 `docs/plans/` 에 둡니다.

## 디렉토리

| 경로 | 용도 |
| --- | --- |
| `AGENTS.md` | 프로젝트 지시문 (이 파일) |
| `AGENTS.override.md` | 개인 설정. 있으면 위 파일 대신 읽힙니다. gitignore 대상 |
| `.agents/skills/` | 스킬 |
| `.codex/config.toml` | 샌드박스·승인 정책. **프로젝트가 신뢰 등록돼야 읽힙니다** |
| `docs/plans/` | 작업 계획 |
| `docs/notes/` | 세션 인수인계 노트 |
